import { HoustonClient } from "./HoustonClient";

export const metadata = {
  title: "Houston Food Reviews | Rick's Café",
  description:
    "The best restaurants in Houston, TX — reviewed with obsessive detail.",
};

export default function HoustonPage() {
  return <HoustonClient />;
}
