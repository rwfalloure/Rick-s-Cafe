import { TacosClient } from "./TacosClient";

export const metadata = {
  title: "Taco Run | Rick's Café",
  description:
    "On a quest to find Houston's best taco. Barbacoa is the constant variable.",
};

export default function TacosPage() {
  return <TacosClient />;
}
